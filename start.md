# RiverLab 德州扑克训练室 - 玩家与开服快速上手指南 (start.md)

欢迎来到 **RiverLab 德扑训练室**！
本项目是一个基于 Web 的完全离线德州扑克训练与竞技对战系统，支持**单人教练 AI 进阶对局**，以及通过**局域网（LAN）或 Tailscale 虚拟专网**实现的多设备跨网络实时联机对决。

---

## 快速目录

1. [环境准备与安装](#1-环境准备与安装)
2. [一键启动服务](#2-一键启动服务)
3. [单机模式游玩](#3-单机模式游玩)
4. [局域网 / Tailscale 跨设备多人对战（核心教程）](#4-局域网--tailscale-跨设备多人对战核心教程)
   - [第一步：放行系统防火墙（主机必做）](#第一步放行系统防火墙主机必做)
   - [第二步：获取主机 IP 地址](#第二步获取主机-ip-地址)
   - [第三步：避免代理软件拦截（非常关键！）](#第三步避免代理软件拦截非常关键)
   - [第四步：其他设备浏览器访问](#第四步其他设备浏览器访问)
5. [联机牌桌核心玩法与特色](#5-联机牌桌核心玩法与特色)
6. [常见问题与排查 (FAQ)](#6-常见问题与排查-faq)

---

## 1. 环境准备与安装

- **操作系统**：Windows / macOS / Linux
- **Node.js**：建议使用 Node.js `22.13.0` 或更高版本
- **代码准备**：
  在项目根目录下打开终端，安装项目依赖：
  ```bash
  npm install
  ```

---

## 2. 一键启动服务

在终端执行以下命令启动开发与联机对战服务器：

```bash
npm run dev:local
```

当终端出现如下信息时，说明服务端已成功在所有网卡上就绪：
```text
  vinext dev  (Vite 8.0.13)

  ➜  Local:   http://localhost:4311/
  ➜  Network: http://100.64.xx.xx:4311/   (Tailscale 专网地址)
  ➜  Network: http://192.168.xx.xx:4311/  (本地路由器局域网地址)
```

---

## 3. 单机模式游玩

启动服务后，在本机浏览器打开：
```text
http://localhost:4311
```
- **单人训练模式**：无需等待其他玩家，直接与具有不同风格的 AI 对战，界面包含第一视角教练分析、出牌建议与实时胜率计算。
- **本机多开模拟联机**：如果想在一台电脑上体验多人联机，可以打开多个**无痕/隐身浏览器窗口**（`Ctrl + Shift + N`）分别访问 `http://localhost:4311`，即可同时模拟房主和多名玩家同台对战！

---

## 4. 局域网 / Tailscale 跨设备多人对战（核心教程）

如果您想在另一台电脑、手机、iPad 上与朋友联机，请按照以下 4 步操作：

### 第一步：放行系统防火墙（主机必做）
Windows Defender 防火墙默认会拦截外部设备对 4311 端口的访问。
- 在**运行服务器的这台电脑**上，按快捷键 `Win + X`，点击 **“终端管理员”**（或搜索 PowerShell -> 右键“以管理员身份运行”）。
- 执行这一行命令即可永久放行：
  ```powershell
  New-NetFirewallRule -DisplayName "RiverLab Poker 4311" -Direction Inbound -LocalPort 4311 -Protocol TCP -Action Allow
  ```

### 第二步：获取主机 IP 地址
- **同一路由器 Wi-Fi 下（家庭/办公室局域网）**：查看终端输出的 `http://192.168.x.x:4311/`。
- **不在同一局域网（使用 Tailscale 异地组网）**：
  - 打开本机的 Tailscale 客户端查看本机 IP（或在终端运行 `tailscale ip -4`），通常为 `100.64.x.x` 格式。

### 第三步：避免代理软件拦截（非常关键！）
> **重要提醒**：如果其他电脑打开了网页但**“点击任何按钮都没反应”**，99% 的原因是**访问端电脑开着梯子/代理软件（Clash、V2Ray、Clash Verge 等）**！
- **原理**：Tailscale 的 `100.x.x.x` 网段没有被代理软件默认识别为内网，导致浏览器把页面内部的 JS 脚本和 WebSocket 发送到了外部代理节点，从而被拦截。
- **解决方法**：
  1. 在**访问端电脑**上**暂时关闭系统代理**，或者退出代理软件。
  2. 或者在代理软件的“绕过代理（Bypass）”列表中添加：`100.*, 100.64.*`。
  3. 修改后，在浏览器中按 **`Ctrl + Shift + R`**（深度强制刷新）打开。

### 第四步：其他设备浏览器访问
在另一台电脑的浏览器地址栏直接输入主机的 IP 地址与端口，例如：
```text
http://100.64.37.127:4311
```
即可直接秒进游戏！

---

## 5. 联机牌桌核心玩法与特色

1. **创建房间与自定规则**：
   - 房主可以自定义**小盲/大盲、初始带入筹码、单次常规思考时间（15s / 20s / 30s）、初始延时卡数量**。
2. **平稳开局机制（房主掌控节奏）**：
   - 房主在大厅点击「🚀 开启牌局」后，全员进入牌桌，看清各自座位与筹码；
   - 此时不会匆忙发牌，**必须等待房主在牌桌上主动点击「开始第一手 🚀」**，系统才正式发牌并开始倒计时。
3. **加时卡（Time-Bank）机制**：
   - 每位玩家拥有固定张数的延时卡。
   - 当思考倒计时**剩余 ≤ 5 秒**时，延时卡按钮点亮高光；
   - 点击使用可**增加单次常规思考时间的整整一倍（例如常规为 20s，点击即 +20s）**，倒计时与进度条瞬间回血！
4. **一局结束与复盘展示**：
   - 每一局分池结束后，系统**不会自动跳过**，而是永久保持结算与亮牌展示界面；
   - 必须等待房主主动点击 **「开始下一手 →」** 才会开启下一场对决，大家有充分的时间聊天、复盘手牌。
5. **旁观者上帝视角**：
   - 旁观席位可自由勾选「👁️ 上帝视角」，实时透视全场底牌，并享受由引擎实时计算的全员胜率矩阵。
6. **交互式无缝游戏 BGM**：
   - 内置 5 首专为德扑场景设计的分轨音乐（大厅准备、翻前对决、河牌圈、加时卡紧迫音效、分池结算）。
   - 底层采用同轴多轨并跑技术，切换音乐时绝不卡顿跳拍，如专业调音台般无缝淡入淡出！

---

## 6. 常见问题与排查 (FAQ)

- **Q: 另一台电脑打不开网址，提示超时？**
  - A: 请检查主机是否执行了[第一步的防火墙放行命令](#第一步放行系统防火墙主机必做)；如果是 Tailscale 联机，在另一台电脑终端 ping 一下主机的 `100.x.x.x` 地址确认网络已连通。
- **Q: 网页能打开看到界面，但点任何按钮都没反应？**
  - A: 参见[第三步](#第三步避免代理软件拦截非常关键)。关闭访问端电脑上的代理软件（Clash / V2Ray 等），然后在浏览器按 `Ctrl + Shift + R` 强制刷新网页即可恢复。
- **Q: 背景音乐没有声音？**
  - A: 现代浏览器禁止网页未发生用户交互时自动出声。只需在网页上任意点击一下（如点击“局域网联机”或桌布），音乐便会自动解锁并同步播放。
